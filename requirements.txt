# 视频 AI 重命名工具依赖
# 安装命令: pip install -r requirements.txt

# Gemini 多模态 AI SDK (新版)
google-genai>=0.5.0

# 本地语音转写
faster-whisper>=1.0.0

# 进度条（可选但推荐）
tqdm>=4.66.0

# 环境变量管理
python-dotenv>=1.0.0

# OpenAI API SDK (用于兼容其他模型后端)
openai>=1.14.0

# GPU 加速依赖 (仅适用于 CUDA 12 环境)
nvidia-cublas-cu12
nvidia-cudnn-cu12
